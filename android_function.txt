========================================  variable ========================================
------------ int


------------ String
String variable = "Java";


// chèn biến
String result = String.format("Hello, %s World!", variable);

String.format("Hello, %.0f World!", double);
 
------------ ArrayList
ArrayList<String> list = new ArrayList<>();

// thêm vào cuối
.add("alo")

// thêm với index
.add(index, element)

// xoá object
.remove(Object)

// xoá với index
remove(index)

// số lượng phần tử
.size()

// kiểm tra rỗng
.isEmpty()

// kiểm tra tồn tại
.contains(Object)

// xoá
.clear()

// lấy dữ liệu
.get(index)

// thay thế
/set(index, element)


========================================  Event Listener ========================================

------------ OnClickListener ------------

object: Button, TextView, ImageView, FloatingActionButton, CardView, ...

// nhấn vào 1 view
.setOnClickListener(new View.OnClickListener() {
    @Override
    public void onClick(View v) {
        // code
    }
});

// nhấn giữ lâu
.setOnLongClickListener(new View.OnLongClickListener() {
    @Override
    public boolean onLongClick(View v) {
        Toast.makeText(MainActivity.this, "Long Clicked!", Toast.LENGTH_SHORT).show();
        return true; // Trả về true nếu xử lý xong
    }
});

------------ OnTouchListener ------------

object: Button, ImageView, ListView, RecyclerView

// chạm vào màn hình (touch event)

.setOnTouchListener(new View.OnTouchListener() {
    @Override
    public boolean onTouch(View v, MotionEvent event) {
        if (event.getAction() == MotionEvent.ACTION_DOWN) {
            v.setBackgroundColor(Color.RED);
        } else if (event.getAction() == MotionEvent.ACTION_UP) {
            v.setBackgroundColor(Color.BLUE);
        }
        return true;
    }
});

------------ OnFocusChangeListener ------------

object: EditText, SearchView, TextView

// Bắt sự kiện khi một View nhận hoặc mất focus

.setOnFocusChangeListener(new View.OnFocusChangeListener() {
    @Override
    public void onFocusChange(View v, boolean hasFocus) {
        if (hasFocus) {
	    // đang focus
            editText.setBackgroundColor(Color.YELLOW);
        } else {
	    // mất focus
            editText.setBackgroundColor(Color.WHITE);
        }
    }
});

------------ OnItemClickListener (cho ListView, GridView, Spinner) ------------

object: ListView, GridView, Spinner

// Bắt sự kiện chọn một item trong danh sách

.setOnItemClickListener(new AdapterView.OnItemClickListener() {
    @Override
    public void onItemClick(AdapterView<?> parent, View view, int position, long id) {
        String selectedItem = (String) parent.getItemAtPosition(position);
        Toast.makeText(MainActivity.this, "Selected: " + selectedItem, Toast.LENGTH_SHORT).show();
    }
});

// Bắt sự kiện nhấn giữ một item trong danh sách

.setOnItemLongClickListener(new AdapterView.OnItemLongClickListener() {
    @Override
    public boolean onItemLongClick(AdapterView<?> parent, View view, int position, long id) {
        Toast.makeText(MainActivity.this, "Long Clicked: " + position, Toast.LENGTH_SHORT).show();
        return true;
    }
});

------------ OnItemSelectedListener (cho Spinner) ------------

object: Spinner

// Bắt sự kiện khi một item trong Spinner được chọn

.setOnItemSelectedListener(new AdapterView.OnItemSelectedListener() {
    @Override
    public void onItemSelected(AdapterView<?> parent, View view, int position, long id) {
        String selected = parent.getItemAtPosition(position).toString();
        Toast.makeText(MainActivity.this, "Selected: " + selected, Toast.LENGTH_SHORT).show();
    }

    @Override
    public void onNothingSelected(AdapterView<?> parent) {
        // Không làm gì nếu không chọn item nào
    }
});

------------ OnScrollListener (cho ListView, RecyclerView) ------------

object: ListView, RecyclerView

// Bắt sự kiện cuộn (scroll) danh sách

.setOnScrollListener(new AbsListView.OnScrollListener() {
    @Override
    public void onScrollStateChanged(AbsListView view, int scrollState) {
        if (scrollState == SCROLL_STATE_IDLE) {
            Toast.makeText(MainActivity.this, "Scroll Stopped", Toast.LENGTH_SHORT).show();
        }
    }

    @Override
    public void onScroll(AbsListView view, int firstVisibleItem, int visibleItemCount, int totalItemCount) {
        // Xử lý khi cuộn
    }
});

------------ OnKeyListener (cho phím bấm) ------------

object: EditText, SearchView, TextView

// Bắt sự kiện khi người dùng bấm phím trên bàn phím
.setOnKeyListener(new View.OnKeyListener() {
    @Override
    public boolean onKey(View v, int keyCode, KeyEvent event) {
        if (event.getAction() == KeyEvent.ACTION_DOWN && keyCode == KeyEvent.KEYCODE_ENTER) {
            Toast.makeText(MainActivity.this, "Enter Pressed!", Toast.LENGTH_SHORT).show();
            return true;
        }
        return false;
    }
});

======================================== POST, GET API (REST) ========================================
Request, Respone API sử dụng Volley trong Android.

string apiURL = http://10.0.2.2:3000/api/auth/login

---------- tạo file json để request (bằng hashMap)

HashMap<String, String> params = new HashMap<>();
params.put("user_name", username);
params.put("password", password);


JsonObjectRequest jsonObjectRequest = new JsonObjectRequest(
		Request.Method.POST, 
		apiLogin,
                new JSONObject(params),
                new Response.Listener<JSONObject>() {
                    @Override
                    public void onResponse(JSONObject response) {
               	    	// lệnh Response
		    }
                },
                new Response.ErrorListener() {
                    @Override
                    public void onErrorResponse(VolleyError error) {
                       // lệnh ErrorResponse
                    }
                }
	    ) {
            	@Override
            	public Map<String, String> getHeaders() throws AuthFailureError {
                    HashMap<String, String> headers = new HashMap<>();
                    headers.put("Content-Type", "application/json");
                    return headers;
            	}
            };


------------ lệnh trong response

try {
	if(response.getBoolean("success")) {
		String token = response.getString("token");
		
		sharedPreClass.setValue_string("token", token);

		Toast.makeText(LoginActivity.this, response.getString("msg"), Toast.LENGTH_SHORT).show();
		// Toast.makeText(RegisterActivity.this, token, Toast.LENGTH_SHORT).show();
		startActivity(new Intent(LoginActivity.this, MainActivity.class));
	}

	progressBar.setVisibility(View.GONE);
} 
catch (JSONException je) {
	je.printStackTrace();
	progressBar.setVisibility(View.GONE);
}

------------ lệnh trong errorResponse

NetworkResponse response = error.networkResponse;

if(error instanceof ServerError && response != null) {

	try {
		String res = new String(response.data, HttpHeaderParser.parseCharset(response.headers, "utf-8"));
		
		JSONObject obj = new JSONObject(res);

		// thông báo
		Toast.makeText(LoginActivity.this, obj.getString("msg"), Toast.LENGTH_SHORT).show();

		boolean error_server = obj.getBoolean("error_server");
		if(!error_server) {
			error_login.setText(getString(R.string.error_login_invalid));
			error_login.setVisibility(View.VISIBLE);
		}

		progressBar.setVisibility(View.GONE);
	} catch (JSONException | UnsupportedEncodingException je) {
		je.printStackTrace();
		progressBar.setVisibility(View.GONE);
	}
}

